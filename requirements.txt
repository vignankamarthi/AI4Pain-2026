# AI4Pain-2026 Python dependencies
# NOTE: NO external LLM SDKs (no anthropic, openai, langchain, llamaindex).
# The mutation operator is the Claude Code session, not a programmatic client.
# NOTE: NO cluster libraries (no paramiko, no fabric, no asyncssh).
# All cluster operations are manual per HIP-D, HIP-E, HIP-F.

# Numerics + data
numpy>=1.26
pandas>=2.1
scipy>=1.12
pyarrow>=15.0

# ML
scikit-learn>=1.4
torch>=2.3
pycatch22>=0.4

# Utilities
tqdm>=4.66

# TDD (per ANTIPATTERNS rule 21: no implementation without failing test first)
pytest>=7.4
pytest-cov>=4.1
